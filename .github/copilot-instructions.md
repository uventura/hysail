Always follow these rules:

1. Always build using the command "./scripts/build.sh"
2. Use ruff to check for linting errors and fix them before committing.
3. Use black to format the code before committing.
4. Create test names using pytest and following the format "test_<functionality>_<expected_behavior>".
